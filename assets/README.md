# Assets

The original website's assets (CSS, JS, images, fonts) are protected from direct download on the live server.

To fully mirror the site, the developer should copy the `assets/` and `images/` folders from the live server using:
- cPanel File Manager
- FTP/SFTP
- Or ask the hosting provider for a backup

The PHP/HTML files in this repo already reference these asset paths.
